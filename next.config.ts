/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    optimizeFonts: true,
  },
  // If you're using a basePath, ensure it starts with a slash
  // basePath: '/your-base-path', // Uncomment if needed
}

module.exports = nextConfig